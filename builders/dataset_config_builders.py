def get_default_config(dataset_name,
                    split_name, 
                    dataset_dir, 
                    batch_size,
                    crop_size,
                    min_resize_value=None,
                    max_resize_value=None,
                    resize_factor=None,
                    min_scale_factor=1.,
                    max_scale_factor=1.,
                    scale_factor_step_size=0,
                    model_variant=None,
                    num_readers=1,
                    is_training=False,
                    should_shuffle=False,
                    should_repeat=False):
    return {
        "dataset_name": dataset_name,
        "split_name": split_name,
        "dataset_dir": dataset_dir,
        "batch_size": batch_size,
        "crop_size": crop_size,
        "min_resize_value": min_resize_value,
        "max_resize_value": max_resize_value,
        "resize_factor": resize_factor,
        "min_scale_factor": min_scale_factor,
        "max_scale_factor": max_scale_factor,
        "scale_factor_step_size": scale_factor_step_size,
        "model_variant": model_variant,
        "num_readers": num_readers,
        "is_training": is_training,
        "should_shuffle": should_shuffle,
        "should_repeat": should_repeat,
    }


def ade20k_default_config(split_name, 
                          dataset_dir, 
                          batch_size,
                          crop_size,
                          min_resize_value=None,
                          max_resize_value=None,
                          resize_factor=None,
                          min_scale_factor=1.,
                          max_scale_factor=1.,
                          scale_factor_step_size=0,
                          model_variant=None,
                          num_readers=1,
                          is_training=False,
                          should_shuffle=False,
                          should_repeat=False):
    return {
        "dataset_name": "ade20k",
        "split_name": split_name,
        "dataset_dir": dataset_dir,
        "batch_size": batch_size,
        "crop_size": crop_size,
        "min_resize_value": min_resize_value,
        "max_resize_value": max_resize_value,
        "resize_factor": resize_factor,
        "min_scale_factor": min_scale_factor,
        "max_scale_factor": max_scale_factor,
        "scale_factor_step_size": scale_factor_step_size,
        "model_variant": model_variant,
        "num_readers": num_readers,
        "is_training": is_training,
        "should_shuffle": should_shuffle,
        "should_repeat": should_repeat,
    }


def voc2012_default_config(split_name, 
                           dataset_dir,
                           batch_size,
                           crop_size,
                           min_resize_value=None,
                           max_resize_value=None,
                           resize_factor=None,
                           min_scale_factor=1.,
                           max_scale_factor=1.,
                           scale_factor_step_size=0,
                           model_variant=None,
                           num_readers=1,
                           is_training=False,
                           should_shuffle=False,
                           should_repeat=False
                           ):
    return {
        "dataset_name": "pascal_voc_seg",
        "split_name": split_name,
        "dataset_dir": dataset_dir,
        "batch_size": batch_size,
        "crop_size": crop_size,
        "min_resize_value": min_resize_value,
        "max_resize_value": max_resize_value,
        "resize_factor": resize_factor,
        "min_scale_factor": min_scale_factor,
        "max_scale_factor": max_scale_factor,
        "scale_factor_step_size": scale_factor_step_size,
        "model_variant": model_variant,
        "num_readers": num_readers,
        "is_training": is_training,
        "should_shuffle": should_shuffle,
        "should_repeat": should_repeat,
    }


def cityscapes_default_config(split_name, 
                              dataset_dir,
                              batch_size,
                              crop_size,
                              min_resize_value=None,
                              max_resize_value=None,
                              resize_factor=None,
                              min_scale_factor=1.,
                              max_scale_factor=1.,
                              scale_factor_step_size=0,
                              model_variant=None,
                              num_readers=1,
                              is_training=False,
                              should_shuffle=False,
                              should_repeat=False):
    return {
        "dataset_name": "cityscapes",
        "split_name": split_name,
        "dataset_dir": dataset_dir,
        "batch_size": batch_size,
        "crop_size": crop_size,
        "min_resize_value": min_resize_value,
        "max_resize_value": max_resize_value,
        "resize_factor": resize_factor,
        "min_scale_factor": min_scale_factor,
        "max_scale_factor": max_scale_factor,
        "scale_factor_step_size": scale_factor_step_size,
        "model_variant": model_variant,
        "num_readers": num_readers,
        "is_training": is_training,
        "should_shuffle": should_shuffle,
        "should_repeat": should_repeat,
    }
